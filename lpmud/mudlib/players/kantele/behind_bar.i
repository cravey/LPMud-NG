# 1 "players/kantele/behind_bar.c"
# 1 "/home/lp2/lpmud/mudlib/room/std.h"







































































































































































# 2 "players/kantele/behind_bar.c"




reset(arg) {  if (arg) return; set_light(1); } short() { if (set_light(0)) return           "Behind the bar"; return "dark room"; } init() { add_action("move", "east"); add_action("drink_beer"); add_verb("drink"); } move() {  call_other(this_player(), "move_player", "east" + "#" +"players/kantele/bar"); return 1; } long(str) { if (set_light(0) == 0){ write("It is dark.\n"); return; }  write(          "You are behind the bar, you can enter the barroom proper to the\n" +          "west, there is a very large barrel of beer here, there should be\n" +          "possible to drink from the tap.....\n"); write("    The only obvious exit is " + "east" + ".\n"); }
# 11 "players/kantele/behind_bar.c"

drink_beer(arg) {
    if (arg != "from tap" && arg != "from the tap")
	return 0;
    if (call_other(this_player(), "drink_alcohol",2)) {
       write("It tasted a bit stale\n");
    }
    return 1;
}
