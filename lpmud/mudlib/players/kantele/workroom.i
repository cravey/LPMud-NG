# 1 "players/kantele/workroom.c"
# 1 "/home/lp2/lpmud/mudlib/room/std.h"







































































































































































# 2 "players/kantele/workroom.c"











reset(arg) { new_reset(); if (!present("money")) { object money; money = clone_object("obj/money"); call_other(money, "set_money",100000); move_object(money,this_object()); } if (arg) return; set_light( 1); } short() { if (set_light(0)) return           "silent workroom"; return "dark room"; } init() { add_action("move1",  "pub" ); add_action("move2", "south");  } move1() {  call_other(this_player(), "move_player",  "pub"  + "#" + "room/pub2" ); return 1; } move2() {  call_other(this_player(), "move_player", "south" + "#" +          "players/kantele/store.c"); return 1; } long(str) { if (set_light(0) == 0){ write("It is dark.\n"); return; }  write(         "Kitiaras silent workroom. \n"); write("There are two obvious exits, " +  "pub"  + " and " + "south" + ".\n"); }
# 18 "players/kantele/workroom.c"

new_reset() {
    if (!present("torch")) {
        object torch;
        torch = clone_object("obj/torch");
        call_other(torch, "set_name", "torch");
        call_other(torch, "set_fuel",9999);
        call_other(torch, "set_weight", 1);
        move_object(torch, this_object());
    }
}
