# 1 "players/morgar/wizworld/chaos.c"

# 1 "/usr/users/jseidman/lpmud/mudlib/room/std.h"







































































































































































# 2 "players/morgar/wizworld/chaos.c"

object fountain;

reset(arg) {  if (arg) return; set_light( 1); }short() {    if (set_light(0))	return 
	 "Chaos";    return "dark room";}init() {    add_action("move"); add_verb( "east");    }move() {        call_other(this_player(), "move_player",  "east" + "#" +"players/morgar/wizworld/chamber");    return 1;}long(str) {    if (set_light(0) == 0){       write("It is dark.\n");       return;    }        write(
	 "This is a viewport into the outer chaos, which existed before LPmud.\n");    write("    The only obvious exit is " +  "east" + ".\n");}

realm() { return("wizard"); }
