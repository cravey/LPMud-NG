# 1 "players/morgar/mazeworld/maze5.c"

# 1 "/usr/users/jseidman/lpmud/mudlib/room/std.h"







































































































































































# 2 "players/morgar/mazeworld/maze5.c"

object mirror;

reset(arg) {  if (arg) return; set_light( 1); }short() {    if (set_light(0))	return 
	 "Dead end corridor";    return "dark room";}init() {    add_action("move"); add_verb( "south");    }move() {        call_other(this_player(), "move_player",  "south" + "#" +"players/morgar/mazeworld/maze1");    return 1;}long(str) {    if (set_light(0) == 0){       write("It is dark.\n");       return;    }        write(
	 "You at the end of an north-south corridor.  The air glows faintly here.\n");    write("    The only obvious exit is " +  "south" + ".\n");}

realm() {
    return("maze");
}
