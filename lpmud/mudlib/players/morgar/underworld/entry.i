# 1 "players/morgar/underworld/entry.c"

# 1 "/usr/users/jseidman/lpmud/mudlib/room/std.h"







































































































































































# 2 "players/morgar/underworld/entry.c"





object mirror;

reset(arg) { extra_reset(); if (arg) return; set_light( 0); }short() {    if (set_light(0))	return 
         "A dark cavern";    return "dark room";}init() {    add_action("move"); add_verb( "west");    }move() {        call_other(this_player(), "move_player",  "west" + "#" +"players/morgar/underworld/brink");    return 1;}long(str) {    if (set_light(0) == 0){       write("It is dark.\n");       return;    }        write(
         "You are at the eastern end of a large undergtound cavern, which is\n" +
         "completely dark.  In the distace you can hear an eerie drip, drip,\n" +
         "drip of water.  The ceiling stretches up out of thre reach of your\n" +
         "light source\n");    write("    The only obvious exit is " +  "west" + ".\n");}

extra_reset() {
    if (!mirror) {
        mirror = clone_object("players/morgar/mirror");
        call_other(mirror, "roomset", this_object());
        move_object(mirror, "players/morgar/hall4");
    }
}

realm() { return("under"); }
