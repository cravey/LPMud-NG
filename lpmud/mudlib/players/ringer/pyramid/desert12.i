# 1 "players/ringer/pyramid/desert12.c"
# 1 "players/ringer/pyramid/../../../room/std.h"







































































































































































# 2 "players/ringer/pyramid/desert12.c"





reset(arg) {  if (arg) return; set_light(1); } short() { if (set_light(0)) return            "A vast desolate desert"; return "dark room"; } init() { add_action("move", "south"); ex_init(); } move() {  call_other(this_player(), "move_player", "south" + "#" +"players/ringer/pyramid/desert5"); return 1; } long(str) { if (set_light(0) == 0){ write("It is dark.\n"); return; }  write( "A desert stretches far into the distance to the south.\n"); write("    The only obvious exit is " + "south" + ".\n"); }
# 11 "players/ringer/pyramid/desert12.c"

ex_init() {
    add_action("search"); add_verb("search");
}

search(str) {
    if (!str) str = "room";
    if (str == "room") {
        write("You find nothing unusual.\n");
        say(call_other(this_player(),"query_name") + " searches the room.\n");
        return 1;
    }
}





