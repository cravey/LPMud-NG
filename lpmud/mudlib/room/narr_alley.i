# 1 "room/narr_alley.c"
# 1 "room/std.h"







































































































































































# 2 "room/narr_alley.c"












reset(arg) {  if (arg) return; set_light( 1); } short() { if (set_light(0)) return  	 "Narrow alley"; return "dark room"; } init() { add_action("move1", "north"); add_action("move2",  "east"); add_action("move3",  "south"); add_action("go_down", "down"); } move1() {  call_other(this_player(), "move_player", "north" + "#" + "room/vill_road1"); return 1; } move2() {  call_other(this_player(), "move_player",  "east" + "#" +  	 "room/bank"); return 1; } move3() {  call_other(this_player(), "move_player",  "south" + "#" +  	 "room/post"); return 1; } long(str) { if (set_light(0) == 0){ write("It is dark.\n"); return; } if (str == "well") { write("You look down the well, but see only darkness.\n"); write("There are some iron handles on the inside.\n"); return; } write( 	 "A narrow alley. There is a well in the middle.\n"); write("There are three obvious exits, " + "north" + ", " +  "east" + " and " +  "south" + ".\n"); }
# 20 "room/narr_alley.c"

go_down() {
    call_other(this_player(), "move_player", "down#room/well");
    return 1;
}

id(str) {
    if (str == "well")
	return 1;
}
